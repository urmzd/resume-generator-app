{
  "name": "resume-generator-e2e-desktop",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npx playwright test",
    "install-browsers": "npx playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
